# Uncomment the next line to define a global platform for your project

platform :ios, '9.3'

target 'NearBy-iOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NearBy-iOS

  pod 'Alamofire', '~> 4.9.0'
  target 'NearBy-iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
